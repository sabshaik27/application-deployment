apiVersion: test.com/v1alpha1
kind: AppClaim
metadata:
  name: application-deploy-test
  labels:
    app-owner: Saba
spec:
  id: apache-test
  compositionSelector:
    matchLabels:
      type: appdeploy
  parameters:
    namespace:  test-automation
    image: httpd
    host: applicationtest.apache12.com
